<form method="post" action="{{ path('app_attachedfile_delete', {'id': attachedfile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attachedfile.id) }}">
    <button class="btn">Delete</button>
</form>
